# Railway: Nixpacks 기본 단계만 사용 (nixpacks.toml 참조)
# buildCommand에 npm ci를 넣지 마세요 — install 단계에서 이미 설치됨
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
