[build]
builder = "NIXPACKS"
# Railway sets NODE_ENV=production during install; force full install for Next.js build
buildCommand = "npm ci --include=dev && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
