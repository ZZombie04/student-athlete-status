# 로컬 개발 (SQLite)
DATABASE_URL="file:./data/dev.db"

# Railway PostgreSQL
# 대시보드에서 Postgres 서비스 Variables의 DATABASE_URL을 앱 서비스에 연결하세요.
# (내부 주소: postgres.railway.internal — Railway 네트워크 안에서만 동작)
# DATABASE_URL=postgresql://postgres:PASSWORD@hostname:5432/railway

NODE_ENV=production
PORT=3000
